# URL du serveur Buzzer existant (Express + Socket.IO), sert aussi l'app Buzzer statique
VITE_SOCKET_URL=https://buzzer.servebeer.com

# Redirect URI OAuth PKCE — doit être enregistrée telle quelle dans le Spotify Developer Dashboard
VITE_SPOTIFY_REDIRECT_URI=http://127.0.0.1:5173

# Client ID Spotify par défaut (fallback si rien n'est renseigné dans l'admin de l'app)
VITE_SPOTIFY_CLIENT_ID=
